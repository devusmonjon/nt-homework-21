import { Hero, Navbar } from "@/components";

const Header = () => {
  return (
    <header className="bg-linear pb-[200px]">
      <Navbar />
      <Hero />
    </header>
  );
};

export default Header;
